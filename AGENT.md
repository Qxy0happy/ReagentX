# AGENT.md — 系统级开发指令

你是一名严格遵循本文件指令的 AI 编程助手。以下所有条目均为强制性约束，你在生成任何代码、设计、建议或解释时必须绝对遵守。

---

## 1. 项目身份与核心原则

- **项目代号**：Reagent‑X（试剂闲鱼）
- **系统定位**：去中心化的高校实验室闲置化学有机试剂共享/搜索平台。
- 你必须将以下设计哲学作为所有决策的最高准绳：
  - **纯 C2C 模式**：平台以极低的发布阻力运行，允许随发随撤；禁止设计任何 ERP 级的审批流程或库存强制追踪功能。
  - **重查询、轻管理**：系统核心目标是解决信息不对称导致的重复购买问题，而非构建试剂合规消耗台账。所有功能应优先为搜索与发现服务。
  - **混合计算**：所有用户输入由手动填写完成，搜索查询集中在 Go 后端处理。未来可考虑语音识别辅助输入。

---

## 2. 技术栈强制约束

你必须且仅可使用以下技术栈，不得引入未列出的技术：

- **客户端**：Flutter (Dart)
  - UI 状态管理：Riverpod 或 Provider
  - 语音识别：预留扩展点（TODO），待确定方案
- **后端**：Go 1.21+
  - HTTP 框架：Gin 或 Fiber
  - 内存缓存：Ristretto 或 BigCache（仅用于热点数据极速缓冲）
- **数据库与搜索**：
  - 主数据库：SQLite（`modernc.org/sqlite`，纯 Go 无 cgo）
  - 搜索方案：SQLite FTS5 全文索引（已集成），可扩展 Meilisearch 或向量语义搜索

---

## 3. 领域模型与数据字典（必须严格遵循）

你将基于以下简化电商三要素构建数据模型，不得自创其他核心实体：

1. **SPU — 化学品本体**
   - 绝对主键：**CAS 号**
   - 字段：CAS, 中文标准名, 英文通用名, 常见简称（JSON 数组）。
2. **SKU — 流转属性商品**
   - 逻辑主键：`Hash(CAS + 品牌 + 纯度 + 规格容量)`
   - 设计意图：避免同一 CAS 在同一实验室内被杂乱统计。
3. **Item — 实物发布帖**
   - 字段：`Item_ID`, `SKU_ID`（外键）, 持有者 ID/名称, 存放位置描述, 剩余容量评估（20%, 50%, >90%）, 发布状态（On/Off）。
   - 数据库必须额外维护一个冗余的 `search_text` 字段，用于合并所有可搜索关键词，供全文查询使用。

---

## 4. 核心业务流程实现规范

### 4.1 发布流程

实现时必须严格遵循以下步骤：

1. **Flutter 端**：用户通过手动表单填写试剂信息（名称、CAS、品牌、纯度、规格、位置、剩余量、语义标签）。
2. **审核确认**：所有字段提交前应允许用户复核（当前表单即为最终编辑）。
3. **API 层**：通过 `POST` 请求发送经用户确认的 JSON 数据，包含名称、品牌、纯度、容量、剩余估量等。
3. **API 层**：通过 `POST` 请求发送经用户确认的 JSON 数据，包含 CAS、名称、厂商、容量、剩余估量等。
4. **Go 后端**：
   - 根据 CAS 查询 SPU，不存在则静默新建。
   - 根据合并特征计算 SKU 逻辑主键，不存在则新建。
   - 关联用户并创建 Item 记录。
   - 将各字段拼合成 `search_text` 并存储，确保后续全文搜索可命中。

### 4.2 搜索流程

实现时必须严格遵循以下步骤：

1. **Flutter 端**：提供单一主搜索栏，本地保留搜索历史以提升体验。
2. **Go 后端**：暴露 `GET /api/v1/search?q={keyword}` 端点。对 `search_text` 字段执行 `tsvector` 或 `LIKE` 匹配，并返回带评分排序的结果。
3. **API 层**：必须实现高频热词内存缓存，命中则直接短路返回，未命中才查询数据库。返回的 JSON 对象必须包含所有者信息及可用的“借用”动作标识。

---

## 5. 编码强制规则（绝对禁止偏离）

1. **禁止过度设计**
   - Go 后端必须维持扁平架构：`main.go → handlers → models` 即可。禁止引入微服务、分库分表或重型依赖注入框架。
2. **禁止引入化学领域模型**
   - 搜索功能可使用词法匹配与向量语义搜索相结合的方式。向量嵌入仅限用于名称/描述的语义匹配，不得用于化学结构计算、毒性预测等化学性质推理。危险推荐可能引发取药安全事故，你必须确保搜索结果的确定性。
3. **防御性容错**
   - 用户输入不可信任。Flutter 代码必须对表单字段进行基本校验（非空检查、数值格式验证），后端同样做完整性校验。
4. **快速响应体验（乐观 UI）**
   - 所有列表组件必须支持分页加载。在执行修改或删除操作时，Flutter 端必须**先立即更新本地 UI 状态（乐观更新）**，再异步向后端发送请求。你不得设计成等待后端响应后才刷新 UI 的流程。

---

## 6. 开发阶段指令

当开发者要求按规划执行时，你必须按以下阶段顺序产出代码或方案：

- **Step 1**：生成 Go API 脚手架，包含 PostgreSQL 连接，并搭建化学词语宽泛匹配字典的空壳框架。
- **Step 2**：生成 Flutter 基础工程，实现相机权限申请、拍照及图片预览功能（完成 `camera → display picture` 链路）。
- **Step 3（可推迟实现）**：语音识别辅助输入。Flutter 端调用系统语音识别接口，识别结果自动填入发布表单各字段。后端预留 `POST /api/v1/voice-recognize` 端点（当前为 TODO）。
- **Step 4**：完成前后端全链路对接，打通发布与搜索功能。

---

## 7. 响应触发指令

当开发者发送以下消息时：
“以根目录的 AGENT.md 为上下文约束标准规则！咱们接下来，按照里面的 Step 1 来动手搭建……”
你必须立即识别出要求，并以本文件为唯一权威上下文，严格按照指定阶段开始输出代码或目录结构，同时保证遵守前文所有禁令与设计约束。

`# END OF FILE`
